# License Files

The root `LICENSE` covers original ArduinoFast source. The files in this
directory are retained for the generated `esptool.js` browser bundle and are
provided under the terms of their respective upstream projects.

Do not treat this directory as a relicensing of third-party code. When the
excluded compiler, SDK, or Arduino library assets are added to a separate build,
their version-specific notices must be added there as well.
